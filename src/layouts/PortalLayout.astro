---
import '@fontsource-variable/dm-sans';
import '@fontsource-variable/playfair-display';

interface Props {
  title?: string;
}
const { title = 'Portal' } = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex, nofollow" />
    <title>{title} — Estrellas del Sur</title>
    <link rel="icon" type="image/svg+xml" href={`${base}/favicon.svg`} />
    <!-- Fonts: self-hosted via @fontsource-variable (imported above) -->
  </head>
  <body>
    <header class="portal-header">
      <div class="portal-header-inner">
        <a href={`${base}/`} class="portal-logo">
          <img src={`${base}/images/logo.svg`} alt="Estrellas del Sur" />
          <span>Estrellas del Sur</span>
        </a>
        <nav class="portal-nav" id="portalNav" style="display:none">
          <a href={`${base}/portal/dashboard/`} class="portal-nav-link">Mi perfil</a>
          <button id="logoutBtn" class="portal-nav-link portal-nav-btn">Cerrar sesión</button>
        </nav>
      </div>
    </header>

    <main class="portal-main">
      <slot />
    </main>

    <footer class="portal-footer">
      <p>© <span id="footerYear"></span> Estrellas del Sur · <a href="mailto:paula@estrellasdelsur.eu">paula@estrellasdelsur.eu</a></p>
    </footer>

    <script>
      import { supabase } from '../lib/supabase.js';

      supabase.auth.onAuthStateChange((event, session) => {
        const nav = document.getElementById('portalNav');
        if (nav) nav.style.display = session ? 'flex' : 'none';
      });

      document.getElementById('footerYear').textContent = new Date().getFullYear();

      document.getElementById('logoutBtn')?.addEventListener('click', async () => {
        await supabase.auth.signOut();
        window.location.href = '/portal/login/';
      });
    </script>
  </body>
</html>

<style>
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { font-size: 16px; }
body {
  font-family: 'DM Sans Variable', 'DM Sans', 'DM Sans Fallback', system-ui, sans-serif;
  background: #fafaf7;
  color: #0a1628;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

:focus-visible {
  outline: 2px solid #0d9488;
  outline-offset: 3px;
}
.portal-header :focus-visible,
.portal-footer :focus-visible {
  outline-color: #f59e0b;
}

.portal-header {
  background: #0a1628;
  border-bottom: 1px solid rgba(255,255,255,.08);
  position: sticky; top: 0; z-index: 100;
}
.portal-header-inner {
  max-width: 1100px; margin: 0 auto;
  padding: .9rem 1.5rem;
  display: flex; align-items: center; justify-content: space-between;
}
.portal-logo {
  display: flex; align-items: center; gap: .6rem;
  text-decoration: none; color: #fff;
  font-family: 'Playfair Display Variable', 'Playfair Display', serif;
  font-weight: 600; font-size: 1.05rem;
}
.portal-logo img { height: 32px; width: auto; }

.portal-nav {
  align-items: center; gap: 1.25rem;
}
.portal-nav-link {
  font-size: .88rem; font-weight: 500; color: rgba(255,255,255,.8);
  text-decoration: none; transition: color .15s;
}
.portal-nav-link:hover { color: #5eead4; }
.portal-nav-btn {
  background: none; cursor: pointer;
  font-family: inherit; font-size: .85rem;
  color: rgba(255,255,255,.75); font-weight: 500;
  padding: .35rem .9rem;
  border: 1px solid rgba(255,255,255,.25);
  border-radius: 999px;
  transition: color .15s, border-color .15s, background .15s;
}
.portal-nav-btn:hover {
  color: #fff;
  border-color: rgba(255,255,255,.5);
  background: rgba(255,255,255,.06);
}

.portal-main {
  flex: 1;
  max-width: 1100px; margin: 0 auto;
  padding: 2.5rem 1.5rem;
  width: 100%;
}

.portal-footer {
  text-align: center;
  padding: 1.5rem;
  font-size: .8rem;
  color: rgba(255,255,255,.55);
  background: #0a1628;
}
.portal-footer a { color: #5eead4; text-decoration: none; }
.portal-footer a:hover { text-decoration: underline; }
</style>
