name: Publicar CTRL+ALT+RESET (programado)

# Fusiona la rama deploy/ctrl-alt-reset en main, construye y despliega el 5 de
# junio de 2026 a las 15:00 UTC (17:00 CEST). Tras publicar, se autoelimina para
# no volver a ejecutarse. Tambien se puede lanzar a mano con "Run workflow".

on:
  schedule:
    - cron: "0 15 5 6 *"
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  publicar:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Comprobar fecha programada
        id: guard
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          elif [ "$(date -u +%Y-%m-%d)" = "2026-06-05" ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Fuera de la fecha programada (5 jun 2026); no se publica."
          fi

      - name: Checkout main
        if: steps.guard.outputs.run == 'true'
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Fusionar deploy/ctrl-alt-reset y limpiar
        if: steps.guard.outputs.run == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin deploy/ctrl-alt-reset
          git merge --no-edit origin/deploy/ctrl-alt-reset
          git rm -q .github/workflows/schedule-ctrl-alt-reset.yml
          git commit -m "feat(ctrl-alt-reset): publicacion programada (5 jun 2026)"
          git push origin main
          git push origin --delete deploy/ctrl-alt-reset || true

      - name: Setup Node.js
        if: steps.guard.outputs.run == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"

      - name: Install dependencies
        if: steps.guard.outputs.run == 'true'
        run: npm ci

      - name: Build
        if: steps.guard.outputs.run == 'true'
        run: npm run build
        env:
          PUBLIC_SUPABASE_URL: ${{ secrets.PUBLIC_SUPABASE_URL }}
          PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.PUBLIC_SUPABASE_ANON_KEY }}

      - name: Upload artifact
        if: steps.guard.outputs.run == 'true'
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

      - name: Deploy to GitHub Pages
        if: steps.guard.outputs.run == 'true'
        id: deployment
        uses: actions/deploy-pages@v4
